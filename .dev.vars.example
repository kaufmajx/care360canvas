# Copy to ".dev.vars" for local Cloudflare Workers preview (`npm run preview`).
# Wrangler loads this file automatically; it is the Workers equivalent of
# .env.local. Do NOT commit the real .dev.vars.
GEMINI_API_KEY=your_gemini_api_key_here
